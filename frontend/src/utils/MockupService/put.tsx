const put = (url: string, ...rest): Object => {
  let res = {
    status: 200,
    data: {},
  };

  switch (url) {
    case url:
      res.data = {};
      break;

    default:
      break;
  }

  return res;
};

export default put;
